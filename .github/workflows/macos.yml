name: MacOS-build

on:
  push:
    branches:
      - 'main'
      - 'develop'
      - 'releases/**'
    tags:
      - '*'
  pull_request:
    branches:
      - 'main'
      - 'develop'
      - 'releases/**'
    tags:
      - '*'

jobs:
  build:
    name: macos-64
    runs-on: "macos-latest"
    steps:
      - uses: actions/checkout@v3

      - name: Building conda environment
        uses: conda-incubator/setup-miniconda@v2
        with:
          activate-environment: ml
          auto-activate-base: false

      - name: Set cache date
        run: echo "DATE=$(date +'%Y%m%d')" >> $GITHUB_ENV

      - uses: actions/cache@v3
        with:
          path: macos
          key: macos-conda-${{ hashFiles('macos.yml') }}-${{ env.DATE }}-${{ env.CACHE_NUMBER }}
          id: cache

      - name: Update environment
        run: conda env update -n ml -f macos.yml
        if: steps.cache.outputs.cache-hit != 'true'

      - name: Sh
        shell: sh -l {0}
        run: |
          conda info
          conda list
      - name: Bash
        shell: bash -el {0}
        run: |
          conda info
          conda list

#      - name: Test IO modules
#        run: python -m pytest -vvv tests/test_io.py
#
#      - name: Test embeddings modules
#        run: python -m pytest -vvv tests/test_io.py
#
#      - name: Test AO modules
#        run: python -m pytest -vvv tests/test_io.py